import 'package:flutter/material.dart';

class Item {
  final String name, size, quantity, price;
  final Color color;

  Item({
    required this.name,
    required this.size,
    required this.quantity,
    required this.price,
    required this.color,
  });
}
